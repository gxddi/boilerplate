# Website Boilerplate

Fast, low level c++ code for running a website server without abstractions.

The server serves website files and handles api calls using non-persistent TCP connections secured with TLS.


## Use

1. Generate certification and keys in creds/
2. Compile and run server.cpp to statically serve files

You can also modify it accordingly to support API calls to your app.
